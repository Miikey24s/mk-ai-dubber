# P12 segment-QA evidence checkpoint - 2026-09-25

Scope: evidence-only closure pass for P12. No changes to pipeline/config/PLAN policy.

## Existing evidence consolidated

- P17 artifacts with segment-QA receipts: 8 fixture runs, 68 checked segments.
- Final P17 flags: 2 / 68.
- Clean P17 runs with zero final flags: 6 / 8.
- Benign non-exact ASR variants that still passed: 23 segments. This is useful false-positive pressure evidence because normal ASR wording variation is not automatically rejected.
- Supplemental retained runs: canonical short 50-segment job plus isolated 53-segment routing run, 103 checked segments total, 4 final flags.

The machine-readable aggregate is `work/benchmarks/p12-evidence-20260925.json`, generated by `tools/p12_evidence.py`.

## Unresolved flagged segments

P17:

- `golden-names-numbers-technical-terms`, segment 9: expected `ba mươi ba phần trăm`, re-ASR heard `53%`; missing critical token is correctly caught. This is a known true-positive regression target.
- `golden-emotional-prosody-stress`, segment 3: timing overflow only, timing ratio 1.1703.

Supplemental retained runs:

- canonical short 50, segment 8: timing overflow, ratio 1.3234.
- canonical short 50, segments 21 and 25: missing critical token `cần`.
- isolated routing 53, segment 9: timing overflow, ratio 1.5250.

The canonical 50-segment and technical-term artifacts were produced before the current acoustic-routing policy was frozen, so their stored action is `text_repair` for missing critical tokens. Re-evaluating those exact rows with current `evaluate_segment_qa()` routes all missing-critical cases to `pronunciation_retry`, while timing-only failures remain `timing_rewrite`. The current policy therefore does not need to change based on this evidence.

## Closure decision

P12 should remain open for human-listening closure only. Machine evidence now shows:

- known serious numeric/critical-token defects are caught;
- benign ASR wording variation can pass without being flagged;
- selective repair remains segment-scoped and action routing is correct under the current policy.

What is still missing is independent listening labels for the remaining flagged segments and a small representative sample of unflagged segments. Without those labels, an audible false-positive rate cannot be claimed. Do not relax thresholds or alter routing just to make the remaining flags disappear.

## Validation

- `uv run pytest -q tests/test_p12_evidence_tool.py tests/test_audio_qa.py` -> 27 passed.
- `python tools/p12_evidence.py --output work/benchmarks/p12-evidence-20260925.json` -> success; artifact integrity checks passed.
